import { gsap } from "@/lib/gsap";

export const loaderTimeline = gsap.timeline({
  paused: true,
  defaults: {
    ease: "power4.out",
  },
});

export const heroTimeline = gsap.timeline({
  paused: true,
  defaults: {
    ease: "power4.out",
  },
});

export const pageTimeline = gsap.timeline({
  paused: true,
  defaults: {
    ease: "power4.out",
  },
});